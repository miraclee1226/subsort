const fontSizes = {
  sm: "12px",
  md: "16px",
  lg: "20px",
  xlg: "24px",
};

const fontWeights = {
  regular: 400,
  semibold: 600,
  bold: 700,
};

const colors = {
  lightGray: "#e5e5e5",
  skyBlue: "#4ABECA",
  };

const theme = {
  fontSizes,
  fontWeights,
  colors
};

export default theme;